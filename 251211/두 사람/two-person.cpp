#include <iostream>
using namespace std;

int main() {
    int ageA, ageB;
    char a, b;

    cin >> ageA >> a >> ageB >> b;

    if((ageA >= 19 && A == 'M') || (ageB >= 19 && b == 'M')) {
        cout << 1;
    } else cout << 0;

    return 0;
}